# Ключ Anthropic API — використовується ТІЛЬКИ на сервері, у /api/parse.
# Скопіюй цей файл у .env.local і встав свій ключ.
# .env.local у .gitignore і ніколи не потрапить у git.
ANTHROPIC_API_KEY=sk-ant-...
